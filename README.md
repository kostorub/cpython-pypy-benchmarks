# cpython-pypy-benchmarks
CPython &amp; PyPy perfomance comparison

Install
```
  python3 -m venv .venv && pip install pip-tools && pip-sync
```
Run
```
  . .venv/bin/activate
  python3 src/app.py
```
